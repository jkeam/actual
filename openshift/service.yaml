apiVersion: v1
kind: Service
metadata:
  name: actual
spec:
  ports:
    - name: web
      protocol: TCP
      port: 5006
      targetPort: web
  type: ClusterIP
  selector:
    app.kubernetes.io/name: actual
